#ifndef VAULT_CONF_H
#define VAULT_CONF_H

/*
 * This is the configuration center for Vault.
 * Functions here are provided for querying and generating the configuration.
 */

int vault_conf_prompt(void); /* Prompts the user interactively to generate a configuration. */

#endif
